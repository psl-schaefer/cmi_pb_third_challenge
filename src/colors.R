
dataset_colors <- c(
  "2020_dataset" = "#4c72b0",  # Soft blue
  "2021_dataset" = "#55a868",  # Muted green
  "2022_dataset" = "#c4b7cb",  # Lavender
  "2023_dataset" = "#dd8452"   # Soft orange
)

infancy_vac_colors <- c(
  "aP" = "#9BDCCD",
  "wP" = "#E7A6E1"
)

sex_colors <- c(
  "Female" = "#F4A4B5",
  "Male" = "#A0C4E8"
)

partition_colors <- c(
  "train" = "#FFD700",  # Gold (contrasts with existing colors)
  "challenge" = "#6A5ACD"  # Slate Blue (provides a distinct contrast)
)
